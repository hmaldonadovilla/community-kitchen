# Local deployment settings for scripts/deploy-apps-script.sh
# Copy to .env.deploy (or .env.deploy.staging / .env.deploy.prod) and update values.

# Optional: environment name (staging | prod). If set, the deploy script loads
# .env.deploy.<env> first and exports CK_CONFIG_ENV for bundle selection.
# DEPLOY_ENV=staging

# Optional: Skip unit tests (set to 1 to skip)
# SKIP_TESTS=1

# Optional: Update an existing /exec deployment
# CLASP_DEPLOYMENT_ID="YOUR_DEPLOYMENT_ID"

# Optional: Create a new deployment if no deployment ID is provided
# CLASP_CREATE_DEPLOYMENT=1

# Optional: Custom deployment description
# CLASP_DEPLOY_DESCRIPTION="Manual deploy"
